laenge_meter = float(input("Bitte Länge in meter eingeben:"))
laenge_foot = laenge_meter * 3.2808
print("Ergebnis:", laenge_foot , "foot")



#value = float(input("How much has the customer spent? "))
#discount = 0.05

#final_cost = value - (value * discount)
#print(round(final_cost, 2))

#The float() method is surrounded by the input() method.
#The float() method converts the string value returned by input() into a floating-point number.
#This allows us to multiply “value” and “discount” because they are both numbers.